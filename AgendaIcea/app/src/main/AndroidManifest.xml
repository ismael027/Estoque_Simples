<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aluno.ufop.br.agendaicea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Agenda Acadêmica"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Edit_Tarefa"></activity>
        <activity android:name=".Lista_Tarefa" />
        <activity android:name=".add_terefa" />
        <activity android:name=".editMatter" />
        <activity android:name=".mainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".addMatter" />
    </application>

</manifest>